import {createTheme} from '@mui/material';

const theme = createTheme({
    palette: {
        primary: {
            main: '#ffca5b',
            contrastText: '#2d2b29'
        },
        secondary: {
            main: '#BF834E',
            contrastText: '#2d2b29'
        }
    }
});

export default theme;